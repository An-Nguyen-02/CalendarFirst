# Copy to .env and fill in values. Used by API (and by docker-compose for the api service).

# Required
DATABASE_URL=postgresql://calsync:calsync@localhost:5434/calsync
JWT_SECRET=your-long-random-secret-change-in-prod

# Optional: JWT expiry
# JWT_ACCESS_EXPIRY=15m
# JWT_REFRESH_EXPIRY=7d

# Stripe (Day 7, test mode)
# STRIPE_SECRET_KEY=sk_test_...
# STRIPE_WEBHOOK_SECRET=whsec_...
# BASE_URL=http://localhost:3000

# Resend email (Day 12)
# RESEND_API_KEY=re_...
# FROM_EMAIL=onboarding@resend.dev

# Google OAuth (Day 13)
# GOOGLE_CLIENT_ID=...apps.googleusercontent.com
# GOOGLE_CLIENT_SECRET=...
# API_PUBLIC_URL=http://localhost:4000
# BASE_URL=http://localhost:3000

# Encryption for calendar refresh tokens (Day 13). 32 bytes, base64.
# Generate: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
# ENCRYPTION_KEY=
