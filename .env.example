# Copy to .env and adjust. Used by api/worker (Day 2+).
# For Docker, api service also reads apps/api/.env (see docker-compose.yml).

# Database
# DATABASE_URL=postgresql://calsync:calsync@localhost:5434/calsync

# Redis (optional)
# REDIS_URL=redis://localhost:6379

# Auth (JWT)
# JWT_SECRET=your-long-random-secret-change-in-prod
# JWT_ACCESS_EXPIRY=15m
# JWT_REFRESH_EXPIRY=7d

# Stripe (test keys, Day 7)
# STRIPE_SECRET_KEY=sk_test_...
# STRIPE_WEBHOOK_SECRET=whsec_...
# STRIPE_PUBLISHABLE_KEY=pk_test_...
# BASE_URL=http://localhost:3000
#   ^ Web app URL for Stripe success/cancel redirects and for Google OAuth post-callback redirect.

# Resend email (Day 12, optional)
# RESEND_API_KEY=re_...
# FROM_EMAIL=onboarding@resend.dev

# Google OAuth (Day 13)
# GOOGLE_CLIENT_ID=...apps.googleusercontent.com
# GOOGLE_CLIENT_SECRET=...
# API_PUBLIC_URL=http://localhost:4000
#   ^ This API's public URL; must match the redirect URI in Google Cloud Console (e.g. .../auth/google/callback).

# Encryption for calendar tokens (Day 13, 32 bytes base64)
# Generate: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
# ENCRYPTION_KEY=...
